version: '3'

services:
  history-etl:
    image: kinecosystem/history-collector:20190313kin3
    #network_mode: host  # for local db
    environment:
      POSTGRES_PASSWORD: ''
      POSTGRES_HOST: ''
      PYTHON_PASSWORD: ''
      FIRST_FILE: '0000003f'
      NETWORK_PASSPHRASE: 'Kin Mainnet ; December 2018'
      MAX_RETRIES: 5
      BUCKET_NAME: 'stellar-core-kin-fed-e50b'
      CORE_DIRECTORY: ''
      LOG_LEVEL: 'INFO'
      APP_ID:
